# نظام التوصيل والتتبع — تشغيل خطوة بخطوة

## ١ — قاعدة البيانات
Supabase → SQL Editor → الصق كل `db/upgrade-delivery.sql` → **Run**
(آمن للتشغيل أكثر من مرة، وما بيلمس أي طلب موجود)

## ٢ — الرفع
ارفع الملفات الجديدة/المعدّلة على GitHub وخلّي Cloudflare ينشر:
- `worker.js` (مسارات `/d/` و `/t/` و `/api/track/osmand`)
- `public/driver.html` · `public/track.html` · `public/assets/delivery-admin.js`
- `public/admin.html` · `public/menu.html`

## ٣ — كيف بيشتغل
| مين | الرابط | شو بيعمل |
|---|---|---|
| صاحب المطعم | `/اسم-المطعم/admin` ← تبويب 🏍️ التوصيل | بيضيف موظفين وأجهزة، بيشوف كل الموتوسيكلات عالخريطة |
| الموظف | `/d/<رمز-خاص>` | «بدء الدوام» ← «استلمت الطلب» ← «في الطريق إليك» ← «تم التوصيل» |
| الزبون | `/t/<رمز-الطلب>` | بيشوف الموتوسيكل عالخريطة لحد ما يوصل — بعدها بينقطع |

الزبون بياخد رابط التتبع: بصفحة «تم الطلب» بالمنيو، وبرسالة الواتساب للمطعم، والموظف فيه يبعتلو ياه بكبسة.

**الإرسال للموظفين:** تلقائي (افتراضي) — كل طلب توصيل نقدي بيوصل فوراً، وطلب Whish بعد الدفع. أو يدوي من الإعدادات.
أول موظف بيكبس «استلمت الطلب» بياخدو، والباقين بيختفي عندهن.

## ٤ — مصادر الموقع
1. **GPS الهاتف** — بدون أي إعداد. بس لازم الصفحة تضل مفتوحة (على الآيفون الموقع بيوقف مع قفل الشاشة).
2. **تطبيق Traccar Client** (مجاني، iOS/Android) — بيشتغل بالخلفية. الموظف بيكبس «جهّز لي رقم للتطبيق» وبيحط الرابط والرقم بالتطبيق.
3. **جهاز GPS للموتوسيكل** — ضيفو من اللوحة برقم الـIMEI:
   - إذا بيدعم HTTP/OsmAnd: «رابط الجهاز» جنبو بالقائمة.
   - أغلب الأجهزة (GT06/Sinotrack/Concox) بتحكي TCP بس — بتحتاج سيرفر Traccar (مجاني، على VPS)،
     وبتحط «رابط التحويل» من اللوحة بإعداد `forward.url` تبع Traccar.

> **Apple AirTag ما بينفع**: Apple ما عندها أي API — الموقع بيظهر بس بتطبيق Find My، وما في طريقة رسمية نقرأو منها.
