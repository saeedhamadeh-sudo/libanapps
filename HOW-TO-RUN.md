# كيف تشغّل النظام — خطوة بخطوة

نظام واحد بيدير تراخيص البرامج الثلاثة:
**منيو المطاعم** · **محاسبة الألمنيوم** · **محاسبة التجارة**

---

## الخطوة ١ — مشروع Supabase

1. supabase.com → **New project**
2. الاسم: `libanapps` · Region: **Frankfurt**
3. احفظ كلمة مرور قاعدة البيانات بمكان آمن

## الخطوة ٢ — تشغيل السكيما

1. **SQL Editor → New query**
2. الصق كل محتوى `db/schema.sql`
3. **Run**

> الملف آمن للتشغيل أكثر من مرة. كل مرة أبعتلك نسخة محدّثة، شغّلها كاملة من أولها — ما بتحذف ولا زبون ولا طلب ولا اشتراك موجود.

## الخطوة ٣ — التخزين

**Storage → New bucket** — أربعة، كلها **Public**:
`logos` · `items` · `banners` · `invoices`

بعدها رجاع على SQL Editor وشغّل السكيما مرة تانية (سياسات التخزين بتنطبق هلق بعد ما صارت الـbuckets موجودة).

## الخطوة ٤ — المفاتيح

**Settings → API** — انسخ:

| المفتاح | وين بينحط |
|---|---|
| `Project URL` | `public/assets/config.js` و `public/assets/license.js` |
| `anon public` | نفس المكانين |
| `service_role` | متغيرات Netlify فقط — **ما بينحط بأي ملف** |

## الخطوة ٥ — الرفع

1. اعمل repository على GitHub اسمه `libanapps` (Private)
2. ارفع كل المجلد متل ما هو
3. Netlify → **Import from Git** → اختار الريبو
4. Build command: فاضي · Publish: `public` · Functions: `netlify/functions`
5. **Environment variables**:

| المتغير | القيمة |
|---|---|
| `SUPABASE_URL` | رابط المشروع |
| `SUPABASE_SERVICE_KEY` | مفتاح service_role |
| `WEBSITE_URL` | `https://libanapps.com` |
| `NODE_ENV` | `production` |

## الخطوة ٦ — حسابك أنت

1. افتح `libanapps.com/super` → ما رح تقدر تدخل بعد
2. اعمل حساب من Supabase → **Authentication → Users → Add user** (إيميلك وكلمة مرور)
3. انسخ الـ`UUID` تبع المستخدم
4. SQL Editor:
   ```sql
   insert into public.platform_admins (user_id) values ('<UUID>')
   on conflict do nothing;
   ```
5. رجاع على `/super` وسجّل دخول — صارت اللوحة تبعك جاهزة

---

## الخطوة ٧ — أول زبون

### مطعم (منيو)
1. `/super` → تبويب **الزبائن** → ضيف الزبون
2. تبويب **إصدار ترخيص جديد**:
   - البرنامج: منيو المطاعم
   - املأ الرابط (`hotzone`) واسم المطعم والواتساب واختار الثيم
   - المدة والسعر
3. **توليد الترخيص** → بيطلع لك المفتاح ورابط المنيو جاهز

بضغطة وحدة بينعمل: المطعم، الاشتراك، المفتاح، وأول قسم بالمنيو.

### برنامج ألمنيوم أو تجارة
1. نفس الشي، بس اختار البرنامج المناسب
2. انسخ المفتاح وابعتو للزبون
3. الزبون بيفتح البرنامج، بيطلع له شاشة التفعيل، بيحط المفتاح

---

## الخطوة ٨ — ربط برنامج الألمنيوم والتجارة

بملف `assets/license.js` عبّي أول سطرين:

```js
var LICENSE_URL  = 'https://xxxx.supabase.co';
var LICENSE_ANON = 'eyJhbGciOi...';
```

وبصفحة البرنامج الرئيسية، قبل أي كود تاني:

```html
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.45.0/dist/umd/supabase.min.js"></script>
<script src="license.js"></script>
<script>
  itechLicense.init({
    product: 'alum',              // أو 'trade'
    onValid: function (info) {
      // الترخيص ساري — شغّل البرنامج
      startApp(info);
    }
  });
</script>
```

### شو بيعمل هالملف
- أول مرة: بيطلب المفتاح، وبيربطه بالجهاز
- بعدها: بيتحقق تلقائياً كل مرة يفتح البرنامج
- آخر ١٥ يوم: شريط تنبيه تحت الشاشة مع رقمك
- بعد الانتهاء: شاشة تجديد
- بدون إنترنت: بيشتغل ٥ أيام على آخر تحقق ناجح، بعدها بيطلب اتصال

### ربط الجهاز
كل مفتاح بينربط بجهاز واحد. إذا الزبون غيّر كمبيوتر، من `/super` اضغط **فك الجهاز** وبيقدر يفعّل من جديد. هيك ما بيقدر يوزّع المفتاح على غيره.

---

## الاستعمال اليومي

| بدك تعمل | وين |
|---|---|
| تشوف مين قرب يخلص اشتراكه | `/super` → فلتر «تنتهي خلال ١٥ يوم» |
| تجدّد لزبون دفع | زر **+شهر** أو **+سنة** |
| توقف زبون ما دفع | زر **إيقاف** — البرنامج بيتقفل عنده فوراً |
| زبون غيّر كمبيوتر | زر **فك الجهاز** |
| مطعم جديد | تبويب إصدار ترخيص |

## ملاحظة على الأمان

المفتاح لحاله ما بيكفي حدا ياخد بيانات زبون تاني — دالة التحقق بترجّع بس معلومات صاحب المفتاح نفسه. وجداول الزبائن والاشتراكات مقفولة كلياً إلا عليك أنت.
